{
  "name": "raynerslanefc-website",
  "version": "1.0.0",
  "private": true,
  "description": "Rayners Lane FC — static site + Netlify functions. No build step; this file only provides function dependencies (web-push for match-alert notifications).",
  "dependencies": {
    "web-push": "^3.6.7"
  }
}
