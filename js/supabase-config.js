/* ─────────────────────────────────────────────────────────────────────────
   RAYNERS LANE FC — Fan account backend (Supabase)

   The publishable key below is a browser key (safe to ship). The ONLY value
   left is the Project URL — find it at:
     Supabase → Settings → Data API → "Project URL"   (or Settings → API)
     It looks like:  https://abcdefghijklmnop.supabase.co
   Paste it into `url` and real accounts go live. Blank `url` = on-device card.
   ───────────────────────────────────────────────────────────────────────── */
window.RLFC_SUPABASE = {
  url: '',  // ← paste your Project URL here (e.g. https://xxxx.supabase.co)
  anonKey: 'sb_publishable_7Iwtr1OlGo-VeysFkLcwcw_JjDU6DWE'
};
