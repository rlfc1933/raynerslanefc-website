/* ─────────────────────────────────────────────────────────────────────────
   RAYNERS LANE FC — Fan account backend (Supabase)

   Real email + username + password logins live in a free Supabase project.
   Paste your two PUBLIC values below and real accounts switch on automatically.
   Leave them blank and the site keeps working with the on-device card (no login).

   Where to find these:  Supabase dashboard → your project → Settings → API
     • url      = "Project URL"   (e.g. https://abcdefgh.supabase.co)
     • anonKey  = "anon public" key  (SAFE to put here — it's a browser key,
                  protected by Row-Level Security. NOT the service_role key.)
   ───────────────────────────────────────────────────────────────────────── */
window.RLFC_SUPABASE = {
  url: '',       // ← paste your Project URL
  anonKey: ''    // ← paste your anon public key
};
